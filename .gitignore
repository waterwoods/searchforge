
!reports/.gitkeep

# Local env and caches
.env
.env.*
.env.current
.python-version
.mypy_cache/
.project_trash/
.pytest_cache/
.cache/
cache/
hf_cache/
torch_cache/
.cache/hf/
.cache/torch/
.venv/
__pycache__/

# Generated artifacts, data, and logs
results/
.runs/
.runs/api.log
.runs/api.pid
.runs/graph.db*
.runs/blobs/
artifacts/
artifacts/tmp/
.trash/
build/
dist/
logs/
experiments/data/
experiments/tmp/
data/
archives/
archives/*.tar.gz

# Node / Python deps and models
node_modules/
# Local HuggingFace / cross-encoder mirrors (large; not versioned; keep after git clean)
# Root only — bare `models/` matches every **/models/ (breaks Cloud Build + fiqa_api.models package).
/models/
hub/
xet/

# Reports (keep explicit keep-file)
reports/
reports/*
reports/artifacts/
reports/raw/
reports/tmp/

# Config write-test temp (Rules Center publishability check)
.write_test_tmp

# Miscellaneous binary/data formats
*.DS_Store
*.coverage
*.log
*.pyc
*.zip
*.tar.gz
*.parquet
*.csv

# Qdrant local storage
.qdrant/
!.qdrant/.gitkeep

# Service logs
services/fiqa_api/logs/

# Runtime PIDs and temp
scripts/.pids/
tmp/
*jobhunter_cache*.sqlite3

# --- generated by push_clean_repo guard ---
.env.cloudrun
.vercel
