# Founder Inspection Notes — Visual Checklist

Use **desktop width** (~1280px or wider) on `/workbench/unified-intake`, light gray background visible.

## 1. Left edge of the big white customer card

Trace a vertical line from the **left border** of the “统一服务入口” white strip down through the tabs into **客户入口**. The **outer left border** of the large white customer portal card should sit on the same vertical line as that chrome — not indented toward center.

## 2. Tab bar vs. content

The **first tab** (“客户入口”) should not look horizontally “ahead of” the white content block below; tab ink bar and content should feel like one stack.

## 3. Inner padding rhythm

Open **客户入口** and compare horizontal breathing room inside the title strip vs. inside the hero/trust cards — spacing should feel **consistent** (18px horizontal on the main customer shell, matching the title strip), not tighter on one and looser on the other.

## Optional

Narrow the window: confirm nothing overflows horizontally and the shell still respects the `1280` max width with side gray margins.
