# Founder Inspection Notes

## Inspect first (order)

1. **`/workbench/unified-intake` — 客户入口 tab** at ~1280–1440px width: portal frame, hero, actions, composer.
2. **办公室工作台 tab** same width: **left queue** + **right paste/case** without excessive empty margin.
3. **Narrow** (~768px): columns stack; queue remains above paste (acceptable).

## “Professional enough”

- The UI should feel **intentionally laid out**, not “developer defaults.”
- Workbench should answer **“what needs attention?”** without scrolling on typical laptops when cases exist.

## Obvious wins after sprint

- Less **empty horizontal void**.
- Clear **product identity strip** at top of the page.
- Workbench **list + detail** rhythm closer to real office software.

## Watch for regressions

- Sticky queue **clipping** on short viewports (scroll inside column).
- Tab contrast on light nested theme inside dark app shell.
