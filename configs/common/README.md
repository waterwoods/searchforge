# Common Platform Base

Shared across all clients and industries.

**Belongs here:**
- Generic workflow defaults (e.g. max_turns_before_handoff)
- Shared fallback labels
- Platform-wide defaults

**Files:**
- `workflow_defaults.json` — Fallbacks for broker_next_step, client_prep, client_reply_draft when category-specific config missing
