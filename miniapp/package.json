{
  "name": "searchforge-miniapp-tests",
  "private": true,
  "version": "0.0.1",
  "type": "commonjs",
  "scripts": {
    "test": "node --import tsx --test tests/**/*.test.ts",
    "test:watch": "node --import tsx --test --watch tests/**/*.test.ts",
    "test:component-gates": "node ../scripts/validate_miniapp_component_gates.mjs",
    "preview:preflight": "node scripts/preview_preflight.mjs",
    "test:config-profile": "node --import tsx --test tests/configProfile.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
