<view class="task-cta">
  <button
    class="btn-primary"
    disabled="{{disabled || loading}}"
    loading="{{loading}}"
    bindtap="onTap"
  >
    {{safeLabel}}
  </button>

  <text wx:if="{{safeDisabledReason}}" class="disabled-reason">{{safeDisabledReason}}</text>
</view>
