════════════════════════════════════════════════════════════════════════
  20 分钟 COMBO 测试 - 完整工具包
════════════════════════════════════════════════════════════════════════

🎯 目标：验证统一入口 + Milvus 路由 + Agent V2/V3 全链路稳定性

────────────────────────────────────────────────────────────────────────
📦 包含的工具
────────────────────────────────────────────────────────────────────────

1. run_combo_20min.sh
   主测试脚本，支持完整 20 分钟 Combo 测试
   
   用法:
     ./scripts/run_combo_20min.sh
     ./scripts/run_combo_20min.sh --with-agent --agent-version v3
     ./scripts/run_combo_20min.sh --qps 4 --window 1200 --save-report

2. demo_combo_quick.sh
   快速 Demo 版本（5 分钟），用于演示和快速验证
   
   用法:
     ./scripts/demo_combo_quick.sh
     ./scripts/demo_combo_quick.sh --with-agent

3. verify_combo_20min.sh
   验证脚本，检查测试结果和系统健康
   
   用法:
     ./scripts/verify_combo_20min.sh
     ./scripts/verify_combo_20min.sh --lite

────────────────────────────────────────────────────────────────────────
🚀 快速开始（3 步）
────────────────────────────────────────────────────────────────────────

# 1️⃣ 启动所有服务
docker compose up -d redis qdrant milvus-standalone
export VECTOR_BACKEND=milvus
export AGENT_MODE=v3
cd services/fiqa_api && uvicorn app_main:app --port 8011 --reload &

# 2️⃣ 运行测试
./scripts/run_combo_20min.sh --with-agent --agent-version v3 --save-report

# 3️⃣ 验证结果
./scripts/verify_combo_20min.sh

────────────────────────────────────────────────────────────────────────
🔧 参数说明
────────────────────────────────────────────────────────────────────────

run_combo_20min.sh 参数:
  --qps N              每秒查询数 (默认: 4)
  --window SEC         窗口时长秒数 (默认: 1200 = 20分钟)
  --with-agent         启用 LabOps Agent
  --agent-version v2|v3  Agent 版本 (默认: v2)
  --save-report        保存详细历史记录
  --vector-backend     向量后端 (默认: milvus)

示例:
  # 基础 20 分钟测试
  ./scripts/run_combo_20min.sh
  
  # 带 Agent V3 + 保存报告
  ./scripts/run_combo_20min.sh --with-agent --agent-version v3 --save-report
  
  # 自定义参数（10 分钟，QPS=8）
  ./scripts/run_combo_20min.sh --qps 8 --window 600 --with-agent

────────────────────────────────────────────────────────────────────────
✅ 验证标准
────────────────────────────────────────────────────────────────────────

必须通过:
  ✓ Redis/Qdrant/Milvus 全部健康
  ✓ 实验运行完整时长无崩溃
  ✓ 错误率 < 1%
  ✓ 报告文件成功生成

优化目标:
  ○ P95 改善 ≥ 10%
  ○ Milvus 路由占比 ≥ 70%
  ○ Agent V3 verdict = PASS 或 EDGE

────────────────────────────────────────────────────────────────────────
📊 期望输出（紧凑版 ≤10 行）
────────────────────────────────────────────────────────────────────────

🎯 Combo Test (20min)
Backend: OK | Router: milvus(87%) | Agent: v3
Queries: ~9600 | Success: 99.8% | Error: 0.2%
P95 Δ: -14.6% | Verdict: PASS
All modules healthy ✅

Detailed reports:
  • reports/LABOPS_COMBO_REPORT.txt
  • reports/LABOPS_AGENT_V3_SUMMARY.txt

────────────────────────────────────────────────────────────────────────
📂 生成的报告文件
────────────────────────────────────────────────────────────────────────

1. reports/LABOPS_COMBO_REPORT.txt
   实验核心报告，包含:
   - A/B 阶段对比
   - P95、QPS、错误率
   - Milvus 路由统计

2. reports/LABOPS_AGENT_V3_SUMMARY.txt (如启用 Agent)
   Agent 分析报告，包含:
   - 决策 (PASS/EDGE/FAIL)
   - LLM 解释（如可用）
   - 代码导航位置

3. reports/LABOPS_AGENT_HISTORY.json (如 --save-report)
   完整历史记录，用于:
   - 趋势分析
   - 断点续传
   - 审计追溯

────────────────────────────────────────────────────────────────────────
🧠 自动验证机制
────────────────────────────────────────────────────────────────────────

测试完成后，可运行以下验证脚本:

1. ./scripts/verify_entry_unification.sh --lite
   验证统一入口健康

2. ./scripts/verify_milvus_lane.sh --lite
   验证 Milvus 路由功能

3. ./scripts/verify_agent_v3.sh
   验证 Agent V3 功能

4. ./scripts/verify_combo_20min.sh
   验证 Combo 测试结果

全部通过标志: ALL PASS ✅

────────────────────────────────────────────────────────────────────────
🎓 进阶用法
────────────────────────────────────────────────────────────────────────

# 多轮测试取平均
for i in {1..3}; do
  echo "=== Run $i/3 ==="
  ./scripts/run_combo_20min.sh --with-agent --agent-version v3
  sleep 60
done

# 对比不同后端
./scripts/run_combo_20min.sh --vector-backend qdrant --window 600
./scripts/run_combo_20min.sh --vector-backend milvus --window 600

# 压力测试（高 QPS）
./scripts/run_combo_20min.sh --qps 10 --window 300

────────────────────────────────────────────────────────────────────────
🐛 故障排查
────────────────────────────────────────────────────────────────────────

问题 1: Docker 服务未启动
  解决:
    docker compose ps
    docker compose up -d redis qdrant milvus-standalone

问题 2: API 无响应 (http://localhost:8011)
  解决:
    lsof -i :8011
    cd services/fiqa_api
    uvicorn app_main:app --port 8011 --reload

问题 3: Agent 失败
  解决:
    pip install PyYAML requests
    cat reports/LABOPS_AGENT_V3_SUMMARY.txt

问题 4: 报告未生成
  解决:
    curl http://localhost:8011/ops/lab/status
    curl http://localhost:8011/ops/lab/report
    ls -lh reports/

────────────────────────────────────────────────────────────────────────
📚 相关文档
────────────────────────────────────────────────────────────────────────

- COMBO_20MIN_QUICKSTART.md        一页纸快速上手
- COMBO_TEST_20MIN_README.md       完整详细文档
- ENTRY_UNIFICATION_PATCH_SUMMARY.md  入口统一说明
- reports/LABOPS_AGENT_MINI_WALKTHROUGH.txt  Agent 教程
- MINI_DASHBOARD_QUICKSTART.md     Dashboard 使用

────────────────────────────────────────────────────────────────────────
✨ 特性亮点
────────────────────────────────────────────────────────────────────────

✅ 一键运行 - 自动健康检查、实验执行、报告生成
✅ Agent 集成 - 可选 V2/V3，自动分析并给出决策
✅ 自动验证 - 内置验证脚本，确保结果可靠
✅ 紧凑输出 - ≤10 行总结，一目了然
✅ 详细报告 - 完整的 A/B 对比、指标统计、代码导航
✅ 容错设计 - 失败自动回退，不影响线上环境

────────────────────────────────────────────────────────────────────────
🚀 现在就开始！
────────────────────────────────────────────────────────────────────────

# 5 分钟快速 Demo
./scripts/demo_combo_quick.sh --with-agent

# 完整 20 分钟测试
./scripts/run_combo_20min.sh --with-agent --agent-version v3 --save-report

════════════════════════════════════════════════════════════════════════

