apiVersion: v1
kind: Service
metadata:
  name: ecommerce-agent-api
  labels:
    app: ecommerce-agent-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ecommerce-agent-api

