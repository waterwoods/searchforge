apiVersion: v1
kind: Service
metadata:
  name: jobhunter-api
  labels:
    app: jobhunter-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: jobhunter-api


