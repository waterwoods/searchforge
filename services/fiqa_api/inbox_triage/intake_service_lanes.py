"""
Bounded Stage-1 service lane markers for durable JSON cases and Postgres structured_payload.

Add-Car is the primary lane in scope; values are plain strings (not a heavy taxonomy).
"""

SERVICE_LANE_ADD_CAR = "add_car"
