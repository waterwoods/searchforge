"""AI Request More drafting V1 — draft-only wording help for the broker.

The deterministic Cap2 missing-information checklist decides *what* is missing.
This package only drafts *how to say it*, then hard-validates the wording and
falls back to the office template. It never mutates Case truth, never creates a
Request More, and never sends anything.
"""
